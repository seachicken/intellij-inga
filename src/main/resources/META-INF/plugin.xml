<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>inga.intellij-inga</id>

    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-name -->
    <name>Inga</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor email="support@yourcompany.com" url="https://www.yourcompany.com">YourCompany</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Simple HTML elements (text formatting, paragraphs, and lists) can be added inside of <![CDATA[ ]]> tag.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-description -->
    <description><![CDATA[
    Enter short description for your plugin here.<br>
    <em>most HTML tags may be used</em>
  ]]></description>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.redhat.devtools.lsp4ij</depends>
    <depends>com.redhat.devtools.intellij.telemetry</depends>

    <!-- Extension points defined by the plugin.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
    <extensions defaultExtensionNs="com.intellij">
        <projectService
                serviceImplementation="inga.intellijinga.IngaSettings"/>
        <toolWindow id="IngaViewer" anchor="bottom" factoryClass="inga.intellijinga.IngaViewer"/>
        <projectConfigurable
                parentId="tools"
                id="inga.intellijinga.IngaConfigurable"
                instance="inga.intellijinga.IngaConfigurable"
                displayName="Inga"
                nonDefaultProject="true"/>
    </extensions>
    <extensions defaultExtensionNs="com.redhat.devtools.lsp4ij">
        <server id="ingaLanguageServer"
                name="Inga"
                factoryClass="inga.intellijinga.IngaLanguageServer">
            <description><![CDATA[
    Some description written in HTML to display it in the LSP consoles view and Language Servers settings.
    ]]>
            </description>
        </server>
        <languageMapping language="JAVA"
                         serverId="ingaLanguageServer"/>
        <languageMapping language="KOTLIN"
                         serverId="ingaLanguageServer"/>
    </extensions>
    <actions>
        <action id="inga.intellijinga.actions.StartAnalysisAction" class="inga.intellijinga.actions.StartAnalysisAction"
                text="Start Inga Analysis"/>
        <action id="inga.intellijinga.actions.StopAnalysisAction" class="inga.intellijinga.actions.StopAnalysisAction"
                text="Stop Inga Analysis"/>

        <group id="inga.intellijinga.IngaGroup" text="Inga" popup="true">
            <reference ref="inga.intellijinga.actions.StartAnalysisAction"/>
            <reference ref="inga.intellijinga.actions.StopAnalysisAction"/>
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </group>
    </actions>
</idea-plugin>